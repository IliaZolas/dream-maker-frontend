import "./notification.scss"

const Notification = () => {
    return (
        <div className="notification">
            <a href="/#">New update just landed</a>
        </div>
    )
}

export default Notification;